{
  "name": "transform",
  "values": [
    {
      "title": "{{ name | capitalize }}",
      "amount": "{{ value }}",
      "category": "{% if type == 'primary' %}main{% else %}secondary{% endif %}",
      "summary": "只处理第一个元素"
    }
  ]
}