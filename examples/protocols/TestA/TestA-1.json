{
  "domain": "test",
  "action": "PROCESS",
  "items": [
    "{# array_dynamic: true #}",
    {
      "name": "{{ name }}",
      "value": "{{ value }}",
      "type": "{{ type }}",
      "priority": "{{ __priority }}",
      "created_time": "{{ __timestamp }}"
    }
  ]
}